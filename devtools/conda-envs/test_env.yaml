name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    #Package dependencies
  - numpy=1.16
  - scipy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - pyscf
    - openfermion
    - gitpython
    - opt_einsum
    - seaborn
  #  - codecov

